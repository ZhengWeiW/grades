<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dream.grades.mapper.ReportMapper">
	<select id="reportTest2"
		resultType="com.dream.grades.entity.StudentScore">
		SELECT * FROM student_score_t
	</select>
	<select id="reportTest"
		resultType="com.dream.grades.entity.StudentScore">
		SELECT * FROM student_score_t
	</select>

</mapper>